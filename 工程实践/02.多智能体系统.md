# 多智能体系统（Multi-Agent System）

### 1、为什么需要Multi-Agent

单 Agent 类比于一个人什么都会做，但都不是很专业；

多 Agent 每个 Agent 都更加专业（Prompt 高度专业化），分工明确，各司其职，协作完成复杂任务。


核心优势：

- 分工明确：每个 Agent 专注于自己的专业领域
- 并行执行：多个 Agent 可以同时工作
- 错误隔离：一个 Agent 出错不影响整体
- 专业化 Prompt：每个 Agent 可以用领域最优提示词


经典场景：

- 软件开发周期（需求 - 设计 - 开发 - 测试 - 部署）
- 内容生产（调研 - 写作 - 编辑 - 发布）
- 投资分析（宏观分析 - 行业分析 - 公司分析 - 风险评估）


三种经典架构

| 架构 | 特点 |
| --- | --- |
| 协作式 | 多 Agent 平等对话，共同决策；AutoGen 的 GroupChat |
| 分层式 | 上级 Agent 委派任务给下级 Agent；CrewAI 的 Crew+Task |
| 竞争式 | 多 Agent 各自生成方案，择优选择；辩论式 Agent |



### 2、LangGraph 双 Agent

需求：

内容创作者撰写内容；内容审核员审计内容，并给出建议

流程：

```
内容 <--
 |     | 不合格
 |     |
审核 ---
 | 合格
 |
输出结果
```

[内容创作者代码实现](../codes/02_01/c03_langgraph_multi_agent.py)



### 3、CrewAI 风格的角色 --- Crew模型

crewAI 采用的是 【Crew = Agent + Task】模型：

- 定义一系列 Agent（角色）
- 定义一系列 Task（任务）
- 将任务分配给 Agent 组成 Crew
- Crew 自动协调执行

[crewAI风格代码](../codes/02_01/c04_crewai_multi_agent.py)

**编排选择：**

多 Agent 是架构选择，不是默认升级。

先建立单 Agent + 工具的质量、延迟和成本基线；只有任务能清晰拆分，角色上下文需要隔离或并行确实有收益时再引入多 Agent。

【？？？？】
LangGraph：应用显式拥有图、状态、checkpoint 和中断点，适合可控工作流

OpenAI Agents SDK：提供 Agent、handoff、guardrail、session 和 tracing 抽象

Responses multi-agent：GPT-5.6 可用的 beta 编排能力，适合可独立并行的子任务

自定义队列/工作流引擎：适合跨服务、长任务、严格重试和审计要求

无论选择哪一层，都要确保：最大子任务数、最大深度、超时/重试、审批边界、每个子Agent的输入输出 trace，以及与单 Agent 基线的同集测评。



### 4、Multi-Agent 的设计原则

1、职责单一：一个 Agent 只做一件事，不要给 Agent 塞太多职责

2、通信标准化：Agent 之间通信用JSON/XML结构化通信，避免自然语言模糊

3、错误隔离：单个 Agent 发生错误不会影响整个系统崩溃，设置重试机制和降级策略

4、可观测性：记录每个 Agent 的输入/输出耗时，便于调试和优化

5、成本控制：用缓存、batching、模型分层来降低成本


Multi Agent 比单 Agent 好在哪？什么时候不该用？

好在：

- 多 Agent 职责单一，单个 Agent 可更加专业化，输出质量更高
- 多 Agent 复杂任务并行执行，效率高
- 系统更模块化、易维护

不适用于：

- 简单任务 （杀鸡不要用牛刀）
- 成本敏感场景（多倍 Agent 调用）
- Agent 间协调开销 > 实际收益



### 5、总结

1、Multi-Agent = 多角色协作｜三种经典 Agent 架构

- 协作式：平等对话，共同决策
- 分层式：上级委派，下级执行
- 竞争式：多方生成，择优选择

2、通信机制

- 共享状态（langGraph）
- 消息传递（AutoGen）
- 任务委派（crewAI）

3、框架实践

- LangGraph：灵活构建自定义 Agent 图
- crewAI：开箱即用的角色-任务模型
- AutoGen：对话驱动的 Multi-Agent

4、设计原则

- 职责单一
- 通信标准化
- 错误隔离
- 可观测性
- 成本可控


项目用了 Multi-Agent吗？为什么？

- 描述场景的复杂度（确实需要多角色协作）
- 说明架构设计（用什么模式【协作｜分层｜竞争】、为什么？）
- 展示效果提升（效率/质量提升了多少）