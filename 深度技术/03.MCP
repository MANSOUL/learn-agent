用户问 MCP 是用来干嘛的,和工具(Function Calling)有什么区别,要一个示例。这是概念性问题,直接回答即可,不需要读代码。
## MCP 是什么

**MCP = Model Context Protocol(模型上下文协议)**,Anthropic 2024 年 11 月推出的开放标准,用来规范 **LLM 应用如何连接外部数据源和工具**。

可以理解为:**"AI 界的 USB-C 接口"** —— 不管什么模型、什么客户端、什么外部能力,只要都遵循 MCP 协议,就能即插即用。

## MCP 想解决的问题

传统 Function Calling 有三个痛点:

1. **每个模型 SDK 接口不一样** —— OpenAI 用 `tools` + `function`,Anthropic 用 `tools` + `input_schema`,各家字段名都不同
2. **每个工具都要手写胶水代码** —— 你想接 GitHub,要写一份 GitHub 工具描述 + 一个调用函数;想接 Slack,又写一份
3. **工具和模型耦合死** —— 工具写死在某个 App 里,换个 App 就要重写

MCP 把这些抽象成统一的客户端-服务器架构,工具/数据源以**独立进程(MCP Server)**的形式存在,任何 MCP 客户端(模型/Agent)都能连。

## MCP vs 传统 Tool(Function Calling)

| 维度 | 传统 Tool / Function Calling | MCP |
|---|---|---|
| 本质 | 模型 API 里的一个字段(schema + 调用函数) | 一个独立运行的服务,通过协议对外暴露能力 |
| 谁来定义工具 | App 开发者,在代码里写死 | MCP Server 自己声明,客户端动态发现 |
| 工具复用 | 换个 App 要重写 | 写一个 MCP Server,所有 MCP 客户端通用 |
| 能提供什么 | 主要是"工具"(function) | 三种能力:**Tools**(可调用函数) / **Resources**(可读资源,如文件) / **Prompts**(预设提示模板) |
| 通信方式 | 进程内函数调用 | stdio 或 HTTP+SSE,跨进程跨网络 |
| 工具发现 | 提前写死 | 客户端连上后**动态列出**可用工具 |
| 状态管理 | 每次调用无状态 | Server 可保持会话状态(如已登录的连接池) |

一句话:**Function Calling 是"模型怎么调函数"的语言协议;MCP 是"工具怎么暴露给模型"的传输协议"**。两者其实是互补关系 —— MCP Server 暴露的能力,最终到客户端那一层,还是会被包装成 Function Calling 的形式喂给模型。

## 具体示例:查 GitHub PR

### 传统 Function Calling 方式

```typescript
// 1. 写 schema
const tools = [{
  type: "function",
  function: {
    name: "get_pr",
    description: "获取 GitHub PR 信息",
    parameters: {
      type: "object",
      properties: {
        owner: { type: "string" },
        repo: { type: "string" },
        pr_number: { type: "number" }
      },
      required: ["owner", "repo", "pr_number"]
    }
  }
}];

// 2. 写实现
async function get_pr({ owner, repo, pr_number }) {
  const res = await fetch(`https://api.github.com/repos/${owner}/${repo}/pulls/${pr_number}`, {
    headers: { Authorization: `Bearer ${process.env.GITHUB_TOKEN}` }
  });
  return res.json();
}

// 3. 调模型,模型决定要不要用,你自己执行
const response = await openai.chat.completions.create({
  model: "gpt-4",
  tools,
  messages: [...]
});

if (response.choices[0].message.tool_calls) {
  const result = await get_pr(response.choices[0].message.tool_calls[0].function.arguments);
  // 再把 result 喂回模型...
}
```

→ 工具 schema、实现、API token、调用流程全写死在 App 里,换个 App 重来。

### MCP 方式

**Step 1:启一个独立的 GitHub MCP Server**(可独立发布到 npm,谁都能用)

```typescript
// github-mcp-server.ts (简化版)
import { Server } from "@modelcontextprotocol/sdk";
import * as github from "./github-client";

const server = new Server({ name: "github-mcp", version: "1.0" });

// Server 自己声明:我能提供这个工具
server.setRequestHandler("tools/list", () => ({
  tools: [{
    name: "get_pr",
    description: "获取 GitHub PR 信息",
    inputSchema: {
      type: "object",
      properties: {
        owner: { type: "string" },
        repo: { type: "string" },
        pr_number: { type: "number" }
      },
      required: ["owner", "repo", "pr_number"]
    }
  }]
}));

// Server 自己实现工具逻辑(包括 token 管理)
server.setRequestHandler("tools/call", async (req) => {
  if (req.params.name === "get_pr") {
    const { owner, repo, pr_number } = req.params.arguments;
    const pr = await github.getPr(owner, repo, pr_number);  // token 在 server 内部
    return { content: [{ type: "text", text: JSON.stringify(pr) }] };
  }
});

server.run({ transport: new StdioServerTransport() });
```

**Step 2:客户端(任何 MCP Client)接进来即可用**

```typescript
import { Client } from "@modelcontextprotocol/sdk";
import { spawn } from "child_process";

// 启动外部 MCP Server 进程
const transport = new StdioClientTransport(
  spawn("node", ["github-mcp-server.ts"])
);
const client = new Client({ name: "my-agent", version: "1.0" });
await client.connect(transport);

// 动态发现这个 server 提供了哪些工具
const { tools } = await client.request({ method: "tools/list" });
// → 自动拿到 get_pr 的 schema,完全不用关心实现

// 然后把 tools 喂给 LLM,LLM 决定调用 → client 转发给 server → 拿结果
```

→ 工具实现、token 管理、schema 都在 Server 端;客户端只负责"发现 + 转发"。换一个 Agent(Claude Desktop / Cursor / 自己写的 Bot)都能直接复用同一个 GitHub MCP Server,零代码改动。

## 实际场景

你现在用的 Trae 里那些 MCP 工具(飞书、GitHub、Notion)就是 MCP 架构 —— Trae 是 MCP 客户端,各个服务的连接器是 MCP Server。Trae 不需要为每个外部服务手写集成代码,只要服务方提供 MCP Server,Trae 就能动态发现并调用。

## 面试一句话总结

> "MCP 是 Anthropic 提出的开放协议,把 LLM 工具调用从『模型 API 内嵌字段』升级成『独立的客户端-服务器架构』。传统 Function Calling 把工具 schema 和实现写死在 App 代码里,换 App 就要重写;MCP 把能力抽到独立的 Server 进程,通过 stdio 或 HTTP 暴露 Tools / Resources / Prompts 三类能力,任何 MCP 客户端都能动态发现并复用。可以理解为 AI 工具生态的 USB-C 标准化接口。"